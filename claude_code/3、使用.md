
# 1、使用


```
cd your-project
claude
```

## 常见命令

### 模式

```
@
```

1. general-purpose (agent) – General-purpose agent for researching complex questions, se…                                                                            
2. statusline-setup (agent) – Use this agent to configure the user's Claude Code status l…
3. Explore (agent) – Fast agent specialized for exploring codebases. Use this wh…
4. Plan (agent) – Software architect agent for designing implementation plans…
5. claude-code-guide (agent) – Use this agent when the user asks questions ("Can Claude...…

```
/
```










## skills
https://code.claude.com/docs/en/skills

### 1、skills下载
#### 自然语言安装
```
帮我安装下 skill，项目地址是：https://github.com/anthropics/skills/blob/mai
n/skills/pptx 
```

### 命令下载

```
claude plugin install superpowers
```
#### 手动安装
下载 skill 安装包，然后直接放到 ～/.claude/skills/


### 2、skills使用



### 3、创建skills


### 4、好用skills集合

1. PPTX
    这个 Skill 让 Claude 直接生成.pptx文件，支持母版、图表、动画
```
claude plugin marketplace add anthropics/skills
claude plugin install document-skills@anthropic-agent-skills
```

MCP Builder
    MCP Builder 把构建过程拆成了四个阶段，引导 Claude 一步步完成：理解 API、设计工具接口、实现、测试。
```
claude plugin marketplace add anthropics/skills
claude plugin install example-skills@anthropic-agent-skills
```

Ralph Loop
    通过 Stop Hook 拦截 Claude 的退出，把同一个任务重新喂给它。

    Claude Code 有个习惯：做到一半觉得差不多了就停下来说“我已经完成了基础框架，你可以在此基础上继续”。Ralph Loop 不让它停。Claude 试图退出，Hook 拦截，检查完成条件，没满足就塞回去。循环往复，直到真正做完。

    简单粗暴，但真的管用。

    用这个有个关键技巧：完成条件要写得越具体越好。做完这个功能不行，Claude 会自己说服自己已经完成了。要写“所有 CRUD 端点可用，测试覆盖率超过 80%，README 包含 API 文档，完成后输出 COMPLETE”。条件越模糊，它越会找理由提前收工。
```
claude plugin install ralph-loop
# 使用示例
/ralph-loop:ralph-loop"实现用户认证模块。完成标准：JWT 登录注册、测试通过、README 更新。完成后输出 COMPLETE"--max-iterations 20 --completion-promise"COMPLETE"
```

openskills 是一个命令行工具，用于安装、管理和同步 Skills。
```
npm install -g openskills
openskills --version
```



### MCP

Playwright MCP